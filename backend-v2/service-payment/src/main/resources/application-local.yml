spring:
  datasource:
    url: jdbc:mysql://localhost:3307/ecommerce_payment?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: sa
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  kafka:
    bootstrap-servers: localhost:9092

cors:
  allowed-origins: http://localhost:3000
