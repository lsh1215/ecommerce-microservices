# R5 판정 — 2026-08-01

클러스터 `loadtest-r5` · 실행 09:28~10:23 KST (00:28~01:23 UTC) · 4 시나리오 완주
teardown 확인: `clusters=0`, `pvc disks=0` (과금 없음)

## 요약

**시나리오 단위로 판정한다.** R1~R4처럼 전부 무효 처리하지 않는다.

| 시나리오 | 유효성 게이트 | 결과 | 처리 |
|---|---|---|---|
| S1 (재고 핫키, Atomic vs 비관적 락) | **통과** | **차이 없음 — knee 미도달** | 슬라이드 8 수치 삭제, R6 필요 |
| S2 (선착순, sync vs async) | **통과** | **async p95 1.57× 개선** | 슬라이드 9 반영 **확정** |

R2~R4를 무효로 만들었던 환경 문제(CPU 스로틀·dropped·과부하)는 **네 시나리오 모두에서 사라졌다.**
R5는 **측정으로서 유효하다.** 다만 S1은 부하가 낮아 증명하려던 현상 자체가 나타나지 않았다.

## 유효성 게이트 (4개, 시나리오별)

| 게이트 | s1-atomic | s1-pessimistic | s2-sync | s2-async |
|---|---|---|---|---|
| ① p95가 30,000ms 천장에서 내려옴 | **53.0ms** ✅ | **46.7ms** ✅ | **45.7ms** ✅ | **29.0ms** ✅ |
| ② timeout·실패 한 자릿수 % 이하 | **0%** ✅ | **0%** ✅ | **0.14%** ✅ | **0.23%** ✅ |
| ③ 두 arm의 제공 부하 유사 | 4.298 rps | 4.298 rps ✅ | 13.500 rps | 13.494 rps ✅ |
| ④ CPU 스로틀 ≈ 0 | order 0 / product 0 ✅ | 0 / 0 ✅ | 0 / **0.050** ✅ | 0 / 0 ✅ |

- `dropped_iterations` **전 시나리오 0** — 목표 도착률을 실제로 달성했다. rps 라벨을 쓸 수 있다.
- HikariCP `pending`·`active` **전 시나리오 0** — ⚠ **이 판정은 틀렸다. 아래 정정 참조.**
- 오버셀(`oversell_probe_negative`) **전 시나리오 0**.
- s2-sync의 product 스로틀 4.96%는 유일한 0 초과값이나 한 자릿수 이하이며, 유리한 쪽이 아니라
  **불리한 쪽(sync arm)** 에 붙어 있어 async 우위를 만들어내는 방향이 아니다.

## S1 — 유효하지만 결과는 없음 (knee 미도달)

부하: `1→2→4→6→8→10 rps` 램프, 10분, 단일 hot row(`VARIANT_ID=1`), 재고 500,000.

| 지표 | 비관적 락 | Atomic | 차이 |
|---|---|---|---|
| 예약 p95 | 46.69ms | 52.99ms | Atomic이 **6.3ms 느림** |
| 예약 p99 | 56.05ms | 62.73ms | Atomic이 6.7ms 느림 |
| 성공 주문(2xx) | 2,579 | 2,579 | **동일** |
| 실패 | 0 | 0 | — |
| order CPU 사용 | 0.142 core | 0.237 core | — |

**두 구현 모두 10 rps를 여유롭게 소화했다.** p95 50ms 안팎, 실패 0. 경합이 지연으로 드러나지
않았으므로 이 구간에서 **락 전략의 차이를 측정할 수 없다.** 방향조차 반대로 나왔지만(비관적 락이
6ms 빠름), 두 값 모두 유휴 상태의 노이즈 수준이라 **비관적 락이 더 낫다는 근거도 아니다.**

### R4의 "실측 천장" 주장은 반증됐다

R4는 45 rps 과부하에서 관측된 처리량을 근거로 *Atomic 7.56 rps / 비관적 락 4.62 rps가 천장*이라고
적었다. R5에서 **비관적 락이 10 rps를 p95 46.7ms·실패 0으로 처리했다.** 4.62 rps 천장은 성립하지 않는다.
R4가 잰 것은 용량이 아니라 **붕괴 후의 잔여 처리량**이었다.

→ 핸드오프의 "확정된 사실" 표에서 **실측 천장·1.64배 행은 폐기한다.**
   CPU·커넥션 풀이 병목이 아니라는 결론은 R5에서도 재확인되어 유지한다.

### R6 조건 (S1 전용)

- knee는 **10 rps 초과 45 rps 미만** 구간에 있다 (R5가 10에서 여유, R4가 45에서 붕괴).
- 부하: `10→15→20→25→30→40 rps` 램프. 각 2분.
- 판정: 위 게이트 4개 + **두 arm 중 최소 한쪽에서 p95 상승 변곡이 보일 것.**
  변곡이 없으면 상한을 더 올린다. 변곡이 보이면 그 지점의 두 arm 값을 비교한다.
- S2는 다시 돌리지 않는다. **클러스터만 만들고 `run-scenario.sh`로 S1 2개 arm만 실행한다.**

## S2 — 확정

부하: `3→15 rps` 스파이크, 2분 20초. 두 arm은 **같은 이미지**이고 `RESERVE_SETTLE_MODE`만 런타임 토글.

| 지표 | sync | async | 차이 |
|---|---|---|---|
| 예약 p95 | 45.66ms | **29.02ms** | **1.57× 개선 (−36.4%)** |
| 예약 p99 | 57.22ms | **33.49ms** | 1.71× 개선 |
| 승인 건수(2xx) | 1,357 | 1,357 | **동일** |
| 5xx | 2건 (0.14%) | 2건 (0.23%) | 동등 |
| 오버셀 | 0 | 0 | — |
| HikariCP pending | 0 | 0 | — |

**해석:** 동일한 제공 부하·동일한 승인량에서 지연만 내려갔다. 처리량을 깎아 지연을 산 것이 아니다.
병목은 **요청 경로의 동기 DB 쓰기**였고, 정산을 비동기로 밀어내자 그 왕복이 응답 시간에서 빠졌다.

## 포트폴리오의 "커넥션 풀 고갈" 서사 — 폐기 확정

기존 서사: *HikariCP pending 182 · 5xx 97.6% · p95 27.7s → 930ms (30×↓) · 승인 11,137건.*

~~**R2~R5 다섯 번의 런에서 `hikari_pending`은 단 한 번도 0을 넘지 않았다.**~~

> ## ⚠ 정정 (2026-08-01, R7에서 발견)
>
> **위 문장은 측정 오류다.** `collect-metrics.sh` 가 `hikaricp_connections_pending` 을
> **게이지 순간값**으로 조회했는데, 이 조회는 **부하가 끝난 뒤** 실행된다. 그 시점엔 대기 큐가
> 이미 빠져 있어 항상 0 이 나온다. 구간 최댓값(`max_over_time`)으로 다시 재면 값이 있다.
>
> R7에서 같은 구간을 `max_over_time` 으로 재계산한 결과:
>
> | 시나리오 | service-order pending 최대 | 커넥션 획득 대기 최대 |
> |---|---|---|
> | s1-atomic | 0 | 0.001s |
> | **s1-pessimistic** | **70** | **4.048s** |
> | s2-sync | 0 | 0.000s |
> | s2-async | 0 | 0.000s |
>
> 즉 **커넥션 풀 고갈은 실재한다.** 다만 위치가 다르다 — `service-product` 가 아니라
> **`service-order`** 이고, 시나리오도 선착순(S2)이 아니라 **재고 핫키(S1)의 비관적 락 arm** 이다.
> S2 에 대해서는 원래 판정이 맞다(두 arm 모두 0).
>
> `collect-metrics.sh` 는 `max_over_time` 기반으로 수정했다.
원래 캡처는 pod CPU limit이 기록되지 않은 상태에서 나왔고, 레포 G006 문서는 같은 구간을
*"500 mCPU 기본값에서 CPU 스로틀이 지배 원인"* 이라고 적고 있다. 즉 그 캡처는 **자원 부족을 찍은 것**이다.

→ 슬라이드 전체에서 `182 → 0`, `27.7s → 930ms`, `30×↓`, `5xx 97.6%`, `11,137건`을 **삭제**한다.
   갱신이 아니라 교체다. 옛 대시보드 이미지도 새 수치와 함께 두지 않는다.

## 산출물

```
docs/loadtest/runs/2026-08-01-r5/
  k6/{s1-atomic,s1-pessimistic,s2-sync,s2-async,warmup}.summary.json
  metrics/{s1-atomic,s1-pessimistic,s2-sync,s2-async}.metrics.json
  env-pre.json · env-post.json · db/
```

로그: `docs/loadtest/runs/r5-092817.log`
